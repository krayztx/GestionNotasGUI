# Copia este archivo como ".env" y completa tus propias credenciales.
# El archivo ".env" real NUNCA debe subirse a Git (ver .gitignore).

DB_URL=jdbc:mysql://localhost:3306/gestion_notas
DB_USER=root
DB_PASSWORD=tu_contrasena_aqui
